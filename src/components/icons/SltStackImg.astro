---
import type { Pixel } from '../../model/model';

export interface Props {
  readonly width: Pixel;
}

const { width } = Astro.props;
---

<img
  loading='eager'
  width={width}
  src='slt-stack.webp'
  alt='stack'
/>
